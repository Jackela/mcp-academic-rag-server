# Simplified Requirements - Core MCP Server Dependencies Only
# Total: ~15 packages (down from 70+)

# Core MCP Protocol
mcp>=1.0.0

# Document Processing (essential only)
PyPDF2>=3.0.0
pytesseract>=0.3.10
Pillow>=10.0.0

# RAG Pipeline (simplified)
haystack-ai>=2.0.0
sentence-transformers>=2.2.0
openai>=1.0.0

# Vector Storage (local only)
faiss-cpu>=1.7.0

# Basic utilities
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Configuration & Validation
pydantic>=2.0.0
jsonschema>=4.0.0
watchdog>=3.0.0

# Logging (simple)
loguru>=0.7.0